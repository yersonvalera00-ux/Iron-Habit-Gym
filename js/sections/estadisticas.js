/**
 * ==========================================================================
 * PESTAÑA: REPORTES Y ESTADÍSTICAS — sections/estadisticas.html
 * initTabEstadisticas() se ejecuta cada vez que dashboard.js inserta este fragmento.
 * ==========================================================================
 */
function initTabEstadisticas() {
  // La gráfica de asistencia semanal es una representación estática de ejemplo
  // (tal como en el prototipo original); la de ingresos sí es dinámica y se
  // recalcula con los datos reales de caja.
  renderizarGraficaIngresos();
}
